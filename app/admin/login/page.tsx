'use client';
import { useState } from 'react';
export default function AdminLogin(){const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=new FormData(e.currentTarget).get('email');const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});if(r.ok)location.href='/admin';else setError('Access denied.');}return <main className="shell form"><div className="panel"><div className="eyebrow">YUGAM SMP / CONTROL CENTER</div><h1>Administrator access.</h1><form onSubmit={submit}><div className="field"><label>AUTHORIZED ADMIN EMAIL</label><input name="email" type="email" autoComplete="email" required/></div>{error&&<p className="error">{error}</p>}<button className="btn primary" type="submit">AUTHENTICATE</button></form></div></main>}
