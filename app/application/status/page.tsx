'use client';
import { useState } from 'react';
export default function Status(){const [result,setResult]=useState<any>();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);const r=await fetch('/api/applications/status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({applicationId:d.get('applicationId'),credential:d.get('credential')})});setResult(await r.json())}return <main className="shell form"><div className="panel"><div className="eyebrow">SECURE PLAYER ACCESS</div><h1>Check your status.</h1><form onSubmit={submit}><div className="field"><label>APPLICATION ID</label><input name="applicationId" placeholder="YUG-APP-000001" required/></div><div className="field"><label>ACCESS CREDENTIAL</label><input name="credential" required/></div><button className="btn primary" type="submit">VIEW APPLICATION</button></form>{result&&<div><p className={result.error?'error':'status'}>{result.error||`${result.applicationId} / ${result.status}`}</p>{result.adminMessage&&<p>{result.adminMessage}</p>}</div>}</div></main>}
