const app = document.querySelector('#app')
const routes = location.pathname.split('/').filter(Boolean)
const state = { form: {}, photo: null }
const esc = value => String(value ?? '').replace(/[&<>\"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[char]))
const date = value => value ? new Date(value).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric' }).toUpperCase() : '—'
function shell(content, active = '') { app.innerHTML = `<div class="site"><header class="nav"><a class="brand" href="/"><span class="brand-icon">Y</span><span><b>YUGAM</b><small>SMP NETWORK</small></span></a><nav><a class="${active === 'apply' ? 'active' : ''}" href="/apply">APPLY</a><a class="${active === 'status' ? 'active' : ''}" href="/application/status">STATUS</a><a href="/admin/login">ADMIN <span>↗</span></a></nav></header>${content}<footer><span>FICTIONAL SERVER DOCUMENT <b>•</b> YUGAM SMP</span><span>REGISTRY ONLINE <i></i></span></footer></div>` }
function home() { shell(`<main class="hero"><div class="hero-copy"><p class="eyebrow">YUGAM SMP <span>/</span> PLAYER SERVICES</p><h1>Your identity<br><em>in the world.</em></h1><p class="lede">Apply for your official YUGAM SMP Player Passport and make your place in the server permanent.</p><div class="actions"><a class="button primary" href="/apply">Apply now <span>↗</span></a><a class="button ghost" href="/application/status">Check application status <span>→</span></a></div><div class="hero-foot"><span><i class="pulse"></i> REGISTRY ONLINE</span><span>SEASON 04 / 2026</span></div></div><div class="hero-visual"><div class="orbit orbit-a"></div><div class="orbit orbit-b"></div><div class="sigil">Y<span>✦</span></div><div class="passport-mini"><div class="mini-top"><b>YUGAM SMP</b><span>PLAYER PASSPORT</span></div><div class="mini-content"><div class="mini-photo"><div class="mini-person"></div></div><div class="mini-lines"><small>PLAYER NAME</small><strong>PLAYER IDENTITY</strong><small>MINECRAFT USERNAME</small><strong>EXAMPLEPLAYER</strong><div class="mini-bottom"><span>YUG-000001</span><b>✓ VERIFIED</b></div></div></div><div class="mini-stripe"></div></div><div class="float-label label-a">OFFICIAL<br><b>PLAYER IDENTITY</b></div><div class="float-label label-b">SECURITY<br><b>LEVEL 04</b></div></div></main><section class="home-strip"><div><b>01</b><span>Apply with your in-world identity</span></div><div><b>02</b><span>Get reviewed by the registry</span></div><div><b>03</b><span>Carry your verified passport</span></div></section>`) }
function apply() { shell(`<main class="form-page"><div class="page-heading"><p class="eyebrow">APPLICATION <span>/</span> 01</p><h1>Enter the registry.</h1><p>Your passport begins with the details that make you, you.</p></div><form id="applicationForm" class="form-panel" enctype="multipart/form-data"><div class="progress"><span class="active">01 <b>IDENTITY</b></span><i></i><span>02 <b>PORTRAIT</b></span><i></i><span>03 <b>REVIEW</b></span></div><div class="form-grid"><label>Full / display name<input name="playerName" required maxlength="80" placeholder="Aquin Anil"></label><label>Minecraft username<input name="minecraftUsername" required maxlength="40" placeholder="ExamplePlayer"></label><label>Minecraft UUID<input name="minecraftUuid" placeholder="Optional — auto-generated if blank"><small>Leave blank if you do not know it.</small></label><label>Discord username<input name="discordUsername" required placeholder="ExampleUser"></label><label>Discord ID<input name="discordId" required placeholder="123456789012345678"></label><label>Server mode<select name="serverMode"><option>Survival</option><option>Anarchy</option></select></label><p class="wide form-note">Application updates and your passport link will be shared through Discord. Keep your Discord ID accurate.</p><label class="wide">Player bio<textarea name="playerBio" required minlength="12" maxlength="600" placeholder="Tell the registry about your role in the world..."></textarea></label><label class="wide upload-label">Passport photograph<div class="upload"><input id="photo" name="photo" type="file" accept="image/jpeg,image/png,image/webp"><div id="uploadPreview"><span>+</span><b>Upload a player portrait</b><small>JPG, PNG or WebP · Max 4MB</small></div></div></label></div><div class="form-end"><p><span>✦</span> Your information is stored securely and only shared with registry staff.</p><button class="button primary" type="submit">Review application <span>→</span></button></div><div id="formMessage" class="message"></div></form></main>`, 'apply')
const form = document.querySelector('#applicationForm'); const fileInput = document.querySelector('#photo'); const preview = document.querySelector('#uploadPreview')
fileInput.addEventListener('change', () => { const file = fileInput.files[0]; if (!file) return; if (file.size > 4 * 1024 * 1024) { fileInput.value = ''; return showMessage('Portrait must be smaller than 4MB.', true) } const reader = new FileReader(); reader.onload = () => { preview.innerHTML = `<img src="${reader.result}" alt="Portrait preview"><span class="change">CHANGE</span>` }; reader.readAsDataURL(file) })
form.addEventListener('submit', async event => { event.preventDefault(); const button = form.querySelector('button'); button.disabled = true; button.innerHTML = 'Submitting <span>...</span>'; try { const response = await fetch('/api/applications', { method: 'POST', body: new FormData(form) }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || 'The application could not be submitted. Please try again.'); appSuccess(data) } catch (error) { button.disabled = false; button.innerHTML = 'Review application <span>→</span>'; showMessage(error.message, true) } }) }
function showMessage(text, error = false, selector = '#formMessage') { const node = document.querySelector(selector); if (node) { node.textContent = text; node.className = `message ${error ? 'error' : 'success'}` } }
function appSuccess(data) { shell(`<main class="success-page"><div class="success-mark">✓</div><p class="eyebrow">APPLICATION <span>/</span> RECEIVED</p><h1>You're on the list.</h1><p>Your application is now waiting for administrator review. Updates and your passport link will be shared through Discord.</p><div class="app-id"><small>APPLICATION ID</small><strong>${esc(data.applicationId)}</strong><span>STATUS <b>PENDING</b></span></div><div class="actions"><a class="button primary" href="/application/${esc(data.applicationId)}">Track application <span>→</span></a><a class="button ghost" href="/">Return home</a></div></main>`) }
async function status() { shell(`<main class="narrow-page"><div class="page-heading"><p class="eyebrow">REGISTRY <span>/</span> STATUS</p><h1>Follow your application.</h1><p>Enter the private details from your confirmation to see the latest update.</p></div><form id="statusForm" class="lookup-panel"><label>Application ID<input name="applicationId" required placeholder="YUG-APP-000001"></label><label>Discord ID<input name="discordId" required placeholder="123456789012345678"></label><button class="button primary" type="submit">Check status <span>→</span></button><div id="lookupMessage" class="message"></div></form><div id="statusResult"></div></main>`, 'status'); document.querySelector('#statusForm').addEventListener('submit', async event => { event.preventDefault(); const response = await fetch('/api/status', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(event.target))) }); const data = await response.json(); if (!response.ok) return showMessage(data.error, true, '#lookupMessage'); document.querySelector('#statusResult').innerHTML = `<div class="status-card"><div><small>APPLICATION ID</small><strong>${esc(data.applicationId)}</strong></div><span class="status-pill ${data.status.toLowerCase().replaceAll(' ', '-')}">${esc(data.status)}</span><div class="status-details"><span>MINECRAFT USERNAME<b>${esc(data.minecraftUsername)}</b></span><span>SUBMITTED<b>${date(data.submissionDate)}</b></span><span>LAST UPDATED<b>${date(data.lastUpdated)}</b></span></div>${data.adminMessage ? `<div class="admin-note"><small>REGISTRY MESSAGE</small><p>${esc(data.adminMessage)}</p></div>` : ''}</div>` }) }
async function adminLogin() { shell(`<main class="login-page"><div class="login-copy"><p class="eyebrow">RESTRICTED AREA <span>/</span> 00</p><h1>Registry<br><em>control.</em></h1><p>Authorized administrator access for the YUGAM SMP player identity system.</p></div><form id="loginForm" class="login-panel"><div class="panel-mark">Y</div><h2>Administrator login</h2><p>Sign in to manage player applications.</p><label>Authorized email<input name="email" type="email" required autocomplete="username"></label><label>Password<input name="password" type="password" required autocomplete="current-password"></label><button class="button primary" type="submit">Authenticate <span>→</span></button><div id="loginMessage" class="message"></div></form></main>`); document.querySelector('#loginForm').addEventListener('submit', async event => { event.preventDefault(); const response = await fetch('/api/admin/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(event.target))) }); const data = await response.json(); if (!response.ok) { const node = document.querySelector('#loginMessage'); node.textContent = data.error; node.className = 'message error'; return } location.href = '/admin' }) }
async function admin() { const auth = await fetch('/api/admin/me').then(r => r.json()); if (!auth.authenticated) return location.href = '/admin/login'; shell(`<main class="admin-page"><div class="admin-heading"><div><p class="eyebrow">REGISTRY <span>/</span> CONTROL CENTER</p><h1>Applications.</h1></div><button id="logout" class="button ghost">Sign out ↗</button></div><div id="adminStats" class="stats"></div><div class="table-panel"><div class="table-head"><h2>All applications</h2><input id="search" placeholder="Search applications..."><select id="filter"><option value="">All statuses</option><option>PENDING</option><option>UNDER REVIEW</option><option>APPROVED</option><option>REJECTED</option><option>NEEDS CHANGES</option></select></div><div id="adminTable"></div></div></main>`); document.querySelector('#logout').onclick = async () => { await fetch('/api/admin/logout', { method: 'POST' }); location.href = '/' }; const rows = await fetch('/api/admin/applications').then(r => r.json()); const render = () => { const query = document.querySelector('#search').value.toLowerCase(); const filter = document.querySelector('#filter').value; const filtered = rows.filter(row => (!filter || row.status === filter) && JSON.stringify(row).toLowerCase().includes(query)); document.querySelector('#adminStats').innerHTML = ['TOTAL', 'PENDING', 'UNDER REVIEW', 'APPROVED', 'REJECTED', 'NEEDS CHANGES'].map(key => `<div class="stat"><small>${key}</small><strong>${key === 'TOTAL' ? rows.length : rows.filter(row => row.status === key).length}</strong></div>`).join(''); document.querySelector('#adminTable').innerHTML = filtered.length ? `<table><thead><tr><th>APPLICATION</th><th>PLAYER</th><th>DISCORD ID</th><th>SUBMITTED</th><th>STATUS</th><th></th></tr></thead><tbody>${filtered.map(row => `<tr><td><b>${esc(row.applicationId)}</b><small>${esc(row.minecraftUsername)}</small></td><td>${esc(row.playerName)}</td><td>${esc(row.discordId)}</td><td>${date(row.submissionDate)}</td><td><span class="status-pill ${row.status.toLowerCase().replaceAll(' ', '-')}">${esc(row.status)}</span></td><td><a class="row-link" href="/admin/applications/${row.applicationId}">OPEN →</a></td></tr>`).join('')}</tbody></table>` : '<div class="empty">No applications match this view.</div>' }; render(); document.querySelector('#search').oninput = render; document.querySelector('#filter').onchange = render }
async function review(id) { const auth = await fetch('/api/admin/me').then(r => r.json()); if (!auth.authenticated) return location.href = '/admin/login'; const response = await fetch(`/api/admin/applications/${id}`); const data = await response.json(); if (!response.ok) return shell(`<main class="narrow-page"><h1>Application not found.</h1></main>`); shell(`<main class="review-page"><a class="back" href="/admin">← All applications</a><div class="review-head"><div><p class="eyebrow">APPLICATION <span>/</span> REVIEW</p><h1>${esc(data.applicationId)}</h1></div><span class="status-pill ${data.status.toLowerCase().replaceAll(' ', '-')}">${esc(data.status)}</span></div><div class="review-grid"><section class="review-panel"><div class="review-photo">${data.photoUrl ? `<img src="${data.photoUrl}" alt="Player portrait">` : '<span>NO PHOTO<br>UPLOADED</span>'}</div><div class="review-fields">${[['Player name', data.playerName], ['Minecraft username', data.minecraftUsername], ['Minecraft UUID', data.minecraftUuid], ['Discord', `${data.discordUsername} · ${data.discordId}`], ['Server mode', data.serverMode], ['Discord ID', data.discordId]].map(([label, value]) => `<div><small>${label}</small><b>${esc(value)}</b></div>`).join('')}<div class="bio"><small>PLAYER BIO</small><p>${esc(data.playerBio)}</p></div></div></section><aside class="decision-panel"><small>REGISTRY ACTION</small><h2>Make a decision.</h2><p>Actions are logged and the player will receive an email update.</p><button class="button primary decision" data-status="APPROVED">Approve passport <span>→</span></button><button class="button danger decision" data-status="REJECTED">Reject application <span>→</span></button><button class="button ghost decision" data-status="NEEDS CHANGES">Request changes <span>→</span></button><div id="decisionMessage" class="message"></div><h3>Review history</h3><div class="history">${data.history.map(item => `<div><span></span><small>${date(item.createdAt)} · ${item.action}</small>${item.message ? `<p>${esc(item.message)}</p>` : ''}</div>`).join('')}</div></aside></div></main>`); document.querySelectorAll('.decision').forEach(button => button.onclick = async () => { const status = button.dataset.status; const message = ['REJECTED', 'NEEDS CHANGES'].includes(status) ? prompt(status === 'REJECTED' ? 'Reason for rejection:' : 'What should the player change?') : ''; if (['REJECTED', 'NEEDS CHANGES'].includes(status) && !message) return; button.disabled = true; const result = await fetch(`/api/admin/applications/${id}/review`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ status, message }) }); const payload = await result.json(); if (!result.ok) { const node = document.querySelector('#decisionMessage'); node.textContent = payload.error; node.className = 'message error'; button.disabled = false } else location.href = '/admin' }) }
async function passport(id, verify = false) { const response = await fetch(`/api/passports/${id}`); const data = await response.json(); if (!response.ok) return shell(`<main class="invalid-page"><div class="invalid-mark">!</div><p class="eyebrow">YUGAM SMP <span>/</span> VERIFICATION</p><h1>Passport not valid.</h1><p>This passport could not be found in the active registry.</p><a class="button ghost" href="/">Return home</a></main>`); shell(`<main class="passport-page"><div class="passport-heading"><div><p class="eyebrow">${verify ? 'PUBLIC VERIFICATION' : 'DIGITAL PASSPORT'} <span>/</span> ${esc(data.passportId)}</p><h1>${verify ? 'Passport verified.' : 'Player passport.'}</h1></div><span class="verified-badge">✓ VERIFIED</span></div><div class="passport-card"><div class="card-watermark">YUGAM SMP</div><div class="card-head"><div class="card-brand"><span class="brand-icon">Y</span><b>YUGAM SMP<small>PLAYER IDENTITY REGISTRY</small></b></div><span>OFFICIAL PLAYER<br><b>IDENTITY DOCUMENT</b></span></div><div class="card-body"><div class="card-photo">${data.photoUrl ? `<img src="${data.photoUrl}" alt="${esc(data.playerName)} portrait">` : '<div class="mini-person"></div>'}</div><div class="card-info"><small>PLAYER NAME</small><h2>${esc(data.playerName)}</h2><div class="card-fields"><span>MINECRAFT USERNAME<b>${esc(data.minecraftUsername)}</b></span><span>PLAYER ID<b>${esc(data.passportId)}</b></span><span>MINECRAFT UUID<b>${esc(data.minecraftUuid)}</b></span><span>SERVER / MODE<b>${esc(data.server)} · ${esc(data.mode)}</b></span><span>DISCORD<b>${esc(data.discordUsername)}</b></span><span>ISSUED<b>${date(data.issueDate)}</b></span></div></div></div><div class="card-foot"><strong>${esc(data.passportId)}</strong><span>SCAN TO VERIFY<br><small>Official YUGAM SMP Player Identity</small></span><div class="fake-qr">▦</div></div></div></main>`) }
if (!routes.length) home(); else if (routes[0] === 'apply') apply(); else if (routes[0] === 'application' && routes[1] === 'status') status(); else if (routes[0] === 'application') status(); else if (routes[0] === 'admin' && routes[1] === 'login') adminLogin(); else if (routes[0] === 'admin' && routes[1] === 'applications') review(routes[2]); else if (routes[0] === 'admin') admin(); else if (routes[0] === 'passport') passport(routes[1]); else if (routes[0] === 'verify') passport(routes[1], true); else home()
